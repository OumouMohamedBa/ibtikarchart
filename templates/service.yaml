apiVersion: v1
kind: Service
metadata:
  name: {{ include "ibtikarchart.fullname" . }}
  labels:
    {{- include "ibtikarchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ibtikarchart.selectorLabels" . | nindent 4 }}
