apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ibtikarchart.fullname" . }}-test-connection"
  labels:
    {{- include "ibtikarchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ibtikarchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
