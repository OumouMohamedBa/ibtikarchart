apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dashgendarmerie.fullname" . }}-test-connection"
  labels:
    {{- include "dashgendarmerie.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dashgendarmerie.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
