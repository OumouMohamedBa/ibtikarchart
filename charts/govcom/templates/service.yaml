apiVersion: v1
kind: Service
metadata:
  name: {{ include "govcom.fullname" . }}
  labels:
    {{- include "govcom.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "govcom.selectorLabels" . | nindent 4 }}
